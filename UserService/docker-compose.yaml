version: '3'

services:
  userservice:
      container_name: userService
      build:
        context: .
        dockerfile: Dockerfile
      ports:
        - "443:443"
      volumes:
        - ./UserService:/home/node/app
  mongodbmicro:
    image: mongo
    container_name: mongodbmicro
    restart: always
    volumes:
      - ./dados_mongo:/data/db